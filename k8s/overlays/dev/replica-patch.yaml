apiVersion: apps/v1
kind: Deployment
metadata:
  name: fincra-app
spec:
  # Two replicas means the ALB always has a second healthy pod to fall back to
  # during a rolling update — one goes down, one keeps serving traffic.
  replicas: 2
